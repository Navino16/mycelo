node_modules/
dist/
*.tsbuildinfo
data/
.env

# A project-level .npmrc is how tokens get committed by accident.
# Credentials belong in ~/.npmrc, outside every repository.
.npmrc

# Subagent-driven development scratch: ledger, briefs, review packages.
.superpowers/
