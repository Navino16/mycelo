kind: enzyme
name: helpdesk
septum: "^0.5"
description: Text and code in one plugin. Test fixture, never distributed.
commands:
  - name: links
    description: Service URLs
    respond: links.text
  - name: rules
    description: House rules
    respond: rules.text
  - name: add
    description: Add a title
    code: handleMutation
  - name: remove
    description: Remove a title
    code: handleMutation
