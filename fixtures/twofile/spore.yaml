kind: enzyme
name: twofile
septum: "^0.5"
description: A plugin in two files, unbundled. Test fixture, never distributed.
commands:
  - name: two
    description: Multi-file check
    code: handleTwo
