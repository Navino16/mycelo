kind: enzyme
name: twofile
septum: "^0.10"
description: A plugin in two files, unbundled. Test fixture, never distributed.
commands:
  - name: two
    description: command.two.description
    code: handleTwo
