kind: enzyme
name: media
septum: "^0.11"
description: Depends on a rhiza and on an any_of group. Test fixture, never distributed.
requires:
  - rhiza: mock
  - any_of: [{ rhiza: nowhere }, { rhiza: mock }]
commands:
  - name: movies
    description: command.movies.description
    code: handleMovies
  - name: where
    description: command.where.description
    code: handleWhere
  - name: count
    description: command.count.description
    code: handleCount
