kind: enzyme
name: help
septum: "^0.11"
description: Lists the commands the caller is authorized to invoke, described and translated. Test fixture, never distributed.
requires:
  - rhiza: mycelium
    scopes: [commands.read]
commands:
  - name: help
    description: command.help.description
    code: handleHelp
