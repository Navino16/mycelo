kind: enzyme
name: help
septum: "^0.8"
description: Lists command names visible through plugins.read. Test fixture, never distributed.
requires:
  - rhiza: mycelium
    scopes: [plugins.read]
commands:
  - name: help
    description: command.help.description
    code: handleHelp
