kind: enzyme
name: admin
septum: "^0.5"
description: Reads the mycelium through a scope. Test fixture, never distributed.
requires:
  - rhiza: mycelium
    scopes: [plugins.read]
commands:
  - name: plugins
    description: List installed plugins
    code: handlePlugins
