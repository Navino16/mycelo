/**
 * Kept equal to package.json's `version` by test/version.test.ts. The core reads it to decide
 * whether a spore's declared range admits the septum actually running (design §10).
 */
export const SEPTUM_VERSION = '0.10.2'
