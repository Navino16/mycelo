command:
  unknown: "unknown command ''{command}''"
  denied: "you are not allowed to use ''{command}''"
  unsupported: "''{command}'' needs {capability}, which channel ''{channel}'' does not provide"
  failed: "command ''{command}'' failed"
context:
  dm: "''{command}'' is only available in a direct message"
  group: "''{command}'' is only available in a group"
api:
  setupRequired: "no UI account exists yet; create one at /api/setup"
  unauthenticated: "no valid session"
  setupConflict: "a UI account already exists"
  loginFailed: "wrong username or password"
  meMissing: "the session principal no longer exists"
  wrongPassword: "the current password is wrong"
  invalidBody: "the request body is invalid"
  invalidQuery: "the query string is invalid"
  invalidRequest: "the request is invalid"
  rateLimited: "too many requests; try again later"
  internalError: "an internal error occurred"
  pluginNotFound: "no plugin named ''{plugin}'' is installed"
  pluginEnableRefused: "plugin ''{plugin}'' could not be enabled"
  commandNotFound: "plugin ''{plugin}'' declares no command named ''{command}''"
  aliasRefused: "the alias for ''{plugin}.{command}'' was refused"
  aliasManifestUnreadable: "plugin ''{plugin}'' has no readable manifest on disk"
  pluginSettingInvalid: "plugin ''{plugin}'' rejected the value given for: {keys}"
  pluginSettingClearRefused: "clearing {keys} would leave plugin ''{plugin}'' with a configuration it refuses; disable it first"
  germinationNotDegraded: "germination can only be retried while the runtime is degraded"
  routeNotFound: "no route for ''{path}''"
  sourceNotFound: "no sporangium source with id ''{id}''"
  sourceOfficial: "the official sporangium cannot be deleted, only disabled"
  sourceOfficialLocation: "the official sporangium cannot be repointed at another location"
  sourceInUse: "''{label}'' still provides installed spores: {spores}"
  sourceLocalBrowse: "this is a local spores directory: there is nothing to browse, its spores are already installed"
  sourceUnreachable: "''{label}'' could not be read"
  sporeNotOffered: "''{label}'' offers no spore named ''{name}''"
  inoculateRefused: "''{name}'' could not be installed from ''{label}''"
  inoculateFailed: "installing ''{name}'' failed; the server log holds the detail"
health:
  timeout: "health() did not answer within {ms}ms"
config:
  schema:
    none: "this plugin takes no configuration"
    noJsonSchema: "this plugin publishes no JSON Schema: configure it by hand"
    notObject: "toJsonSchema() did not return an object"
    conversionFailed: "the schema cannot be converted: {detail}"
    notInstalled: "no plugin named ''{name}'' is installed"
    loadFailed: "the spore ''{name}'' could not be loaded: {detail}"
    absent: "no spore named ''{name}'' is present on disk"
