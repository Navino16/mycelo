yes: "yes"
no: "no"
none: "none"
cancelled: "cancelled"
error: "error"
unknown: "unknown"
refusal:
  plugin:
    notInstalled: "plugin ''{plugin}'' is not installed"
    notOnDisk: "no spore named ''{plugin}'' is present on disk"
    unreadableManifest: "spore ''{plugin}'' has an unreadable manifest: {detail}"
    septumIncompatible: "spore ''{plugin}'' {detail}"
    loadFailed: "spore ''{plugin}'' failed to load: {detail}"
    settingUndeclared: "plugin ''{plugin}'' declares no {count, plural, one{setting} other{settings}}: {keys}"
  role:
    notFound: "role ''{role}'' does not exist"
    exists: "role ''{role}'' already exists"
    builtin: "role ''{role}'' is builtin and cannot be changed"
    isDefault: "role ''{role}'' is the default role and cannot be deleted"
    nameEmpty: "a role name cannot be empty"
    patternDuplicate: "pattern ''{pattern}'' is listed twice"
  person:
    notFound: "no person with id ''{id}''"
  config:
    invalidType: "expected {expected}"
    tooSmall: "{origin, select, string{must be at least {minimum} characters long} array{must have at least {minimum} items} other{must be at least {minimum}}}"
    tooSmallExclusive: "{origin, select, string{must be longer than {minimum} characters} array{must have more than {minimum} items} other{must be greater than {minimum}}}"
    tooBig: "{origin, select, string{must be at most {maximum} characters long} array{must have at most {maximum} items} other{must be at most {maximum}}}"
    tooBigExclusive: "{origin, select, string{must be shorter than {maximum} characters} array{must have fewer than {maximum} items} other{must be less than {maximum}}}"
    invalidFormat: "is not a valid {format}"
    invalidValue: "must be one of: {values}"
    notMultipleOf: "must be a multiple of {divisor}"
    unrecognizedKeys: "declares no setting named: {keys}"
    issueAt: "{field}: {cause}"
    incomplete: "configuration is incomplete: {issues}"
    validationThrew: "configuration is incomplete: validating it threw: {detail}"
    undeclaredSecrets: "configuration declares {count, plural, one{a secret} other{secrets}} {keys} the schema does not have"
  germination:
    reservedDomain: "''{plugin}'' is a reserved translation domain"
    reservedName: "the name ''mycelium'' is reserved for the core"
    duplicateName: "name ''{plugin}'' is already claimed by the spore at ''{claimant}'' (duplicate at ''{duplicate}'')"
    requiredRhizaMissing: "requires rhiza ''{rhiza}'', which is not installed"
    requiredRhizaWrongKind: "requires rhiza ''{rhiza}'', which is kind ''{kind}'', not a rhiza"
    anyOfNoneInstalled: "requires one of rhiza {alternatives} — none is installed"
    dependencyDormant: "requires rhiza ''{rhiza}'', which is dormant: {cause}"
    anyOfDependencyDormant: "requires one of rhiza {alternatives}; ''{chosen}'' was chosen and is dormant: {cause}"
    scopeNotMounted: "requires mycelium scope ''{scope}'', which this core does not mount"
    scopeLaterPhase: "requires mycelium scope ''{scope}'', which arrives in phase {phase}"
    catalogFailed: "its translation catalogue could not be loaded: {detail}"
    moduleCreateThrew: "create() threw: {detail}"
    invalidManifest: "invalid manifest at ''{path}'': {detail}"
    invalidManifestNoPath: "invalid manifest: {detail}"
    createNotObject: "create() returned {got}, expected an object"
    createMissingMethods: "create() returned no {missing}"
    rhizaNoApi: "create() returned no api — enzymes would resolve undefined through ctx.rhiza()"
    enzymeNoHandlersObject: "create() returned no handlers object"
    handlersMissing: "handlers has no function for: {missing}"
    startStopMismatch: "start() and stop() must be both present or both absent"
    inhibitorNoInspect: "create() returned no inspect()"
    methodNotCallable: "{method} is present but not callable"
    capabilityUnimplemented: "manifest declares group_membership but there is no listGroupMembers()"
    capabilityUndeclared: "listGroupMembers() exists but the manifest does not declare group_membership"
  startup:
    hyphaConnectFailed: "connect() failed: {detail}"
    hyphaListenFailed: "listen() failed: {detail}"
    startFailed: "start() failed: {detail}"
